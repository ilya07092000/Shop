extends ../layouts/main-layout.pug

block content
    main
        if orders.length <= 0
            h1 Nothing there!
        else 
            ul
                each order in orders
                    li 
                        p #{order.id}
                        ul  
                            each product in order.products
                                li 
                                    p #{product.title} (#{product.orderItem.quantity})